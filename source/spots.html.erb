---
title: Find the right spot
---

<div class="container text-center">
  <div class="row">
   <h1>Our spots</h1>
    <% data.spots.each do |spot| %>

      <!-- HTML div that we want to repeat -->
      <div class="col-xs-12 col-sm-6 col-md-4 text-center">
        <div class="row">
          <div class="col-xs-6 col-sm-12">
            <%= image_tag spot.picture, class: "img-circle" %>
          </div>
          <div class="col-xs-6 col-sm-12">
            <h2>
              <%= spot.name %>
              <span class="label label-<%= spot.color %>"><%= spot.price %></span>
            </h2>
          <!--   <p><%= spot.summary %></p> -->
          </div>
        </div>
      </div>

    <% end %>
  </div>
</div>